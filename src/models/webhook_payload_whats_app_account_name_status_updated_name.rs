/*
 * Zernio API
 *
 * API reference for Zernio. Authenticate with a Bearer API key. Base URL: https://zernio.com/api  Versioning and deprecation: all endpoints are versioned in the URL path (current version: /v1). Breaking changes only ship in a new path version; existing versions keep working. Deprecated operations are marked 'deprecated: true' in this spec and announced in the changelog (https://zernio.com/changelog) before removal.  Errors: every 4xx/5xx response is application/json with a machine-readable 'code' and a human-readable 'error' message (see the ErrorResponse schema).
 *
 * The version of the OpenAPI document: 1.0.4
 * Contact: support@zernio.com
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct WebhookPayloadWhatsAppAccountNameStatusUpdatedName {
    /// Normalized from Meta's `decision` (REJECTED -> DECLINED, DEFERRED -> PENDING_REVIEW; the review is still open on DEFERRED, not a rejection).
    #[serde(rename = "status")]
    pub status: Status,
    /// The display name Meta reviewed. Null if Meta did not send one.
    #[serde(rename = "requestedName", deserialize_with = "Option::deserialize")]
    pub requested_name: Option<String>,
    /// Meta's free-form decline reason. Null on approval, or when Meta sends the literal string \"NONE\".
    #[serde(rename = "rejectionReason", deserialize_with = "Option::deserialize")]
    pub rejection_reason: Option<String>,
    /// The phone number this review is for, as Meta reported it.
    #[serde(
        rename = "displayPhoneNumber",
        deserialize_with = "Option::deserialize"
    )]
    pub display_phone_number: Option<String>,
}

impl WebhookPayloadWhatsAppAccountNameStatusUpdatedName {
    pub fn new(
        status: Status,
        requested_name: Option<String>,
        rejection_reason: Option<String>,
        display_phone_number: Option<String>,
    ) -> WebhookPayloadWhatsAppAccountNameStatusUpdatedName {
        WebhookPayloadWhatsAppAccountNameStatusUpdatedName {
            status,
            requested_name,
            rejection_reason,
            display_phone_number,
        }
    }
}
/// Normalized from Meta's `decision` (REJECTED -> DECLINED, DEFERRED -> PENDING_REVIEW; the review is still open on DEFERRED, not a rejection).
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum Status {
    #[serde(rename = "APPROVED")]
    Approved,
    #[serde(rename = "DECLINED")]
    Declined,
    #[serde(rename = "PENDING_REVIEW")]
    PendingReview,
}

impl Default for Status {
    fn default() -> Status {
        Self::Approved
    }
}
